#ifndef REPLACE_H
#define REPLACE_H

#include <sstream>
#include <fstream>
#include <iostream>
#include <string>

class strings
{
    private:
        std::string s1;
        std::string s2;
    public:
        strings( void );
        ~strings( void );
};

#endif